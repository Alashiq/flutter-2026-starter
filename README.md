# Hakem - Flutter Starter 2026 🚀

مشروع Flutter احترافي مبني على معمارية منظمة وقابلة للتوسع (Scalable Architecture)، مصمم ليناسب المشاريع المتوسطة والكبيرة بأسلوب الشركات الكبرى.

---

## 🏗️ معمارية المشروع (Project Architecture)

يتبع المشروع نمط **Feature-First Architecture** مع الفصل الكامل بين المنطق (Logic) وواجهة المستخدم (UI).

### 📂 هيكلية المجلدات (Folder Structure)
- **`lib/core`**: يحتوي على الأكواد المشتركة (تنسيقات، أدوات، إعدادات الشبكة، والـ Widgets العامة).
- **`lib/features`**: يحتوي على "المنطق" الخاص بكل ميزة (Models, Actions/Mixins, Controllers).
- **`lib/ui`**: يحتوي على شاشات العرض والوجت الخاصة بكل شاشة.

---

## ⚡ التقنيات الأساسية (Core Stack)

1.  **إدارة الحالة (State Management)**: 
    - استخدام **GetX** لإدارة الحالة وحقن الاعتماديات (Dependency Injection).
    - تعزيز الأمان باستخدام **Sealed Classes** (`ApiState`) لتمثيل حالات الـ API بشكل قطعي.

2.  **الشبكة (Networking)**:
    - **`ApiClient`**: مجمع (Wrapper) ذكي فوق مكتبة `http` يدعم الفصل بين الطلبات المصرحة (`getAuth`) والعامة (`get`).
    - **`ApiHandler`**: معالج مركزي يقوم بتحويل استجابات السيرفر وأكواد الخطأ (404, 401, 500) إلى حالات مفهومة للتطبيق.

3.  **فصل الأكشنز (Action Pattern)**:
    - يتم جلب البيانات عبر **Mixins** منفصلة (مثل `LoadListCityMixin`) مما يضمن بقاء الـ Controller نظيفاً وسهل الاختبار.

---

## 🎨 واجهة المستخدم الذكية (Smart UI)

يعتمد المشروع على نظام الـ **Views المركزية** لتقليل تكرار الكود:

-   **`ApiViewOne<T>`**: مخصص لعرض عنصر واحد، يتولى تلقائياً إظهار (Loading, Error, Empty) بناءً على حالة الـ API.
-   **`ApiViewMulti<T>`**: مخصص لعرض القوائم، مع دعم مدمج لحالت "لا توجد نتائج" وإعادة المحاولة.
-   **`BaseScreenStatus`**: وجت مركزي لتوحيد التصميم البصري لرسائل الخطأ والتنبيهات بأسلوب **Modern Flat Design**.

---

## 🛠️ كيف تبدأ؟ (How to Start)

1.  **تثبيت الحزم**:
    ```bash
    flutter pub get
    ```

2.  **التشغيل بناءً على البيئة (Flavors)**:
    - للتطوير: `flutter run --target lib/main_dev.dart`
    - للإنتاج: `flutter run --target lib/main_prod.dart`

---

## 📝 ملاحظات للمبرمجين
- عند إضافة ميزة جديدة، ابدأ بإنشاء الـ **Model**، ثم الـ **Mixin** الخاص بالأكشن، ثم الـ **Controller**.
- استخدم دائماً `ApiViewOne` أو `ApiViewMulti` في الـ UI لضمان تجربة مستخدم موحدة واحترافية.

---
*تم بناء هذا الأساس ليكون مرجعاً للجودة والكفاءة البرمجية.* ✨
